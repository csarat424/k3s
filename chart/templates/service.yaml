apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: { { .Values.service.port } }
      targetPort: 5001
  type: { { .Values.service.type } }
