apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port | quote }}  # Quote the value to avoid parsing issues
      targetPort: 5000
  type: {{ .Values.service.type | quote }}
